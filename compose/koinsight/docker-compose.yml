name: koinsight
services:
  koinsight:
    container_name: koinsight
    image: ghcr.io/georgesg/koinsight:latest
    restart: unless-stopped
    ports:
      - "${PORT}:3000"
    volumes:
      - /persist/koinsight:/app/data
    networks:
      - proxy

networks:
  proxy:
    external: true
