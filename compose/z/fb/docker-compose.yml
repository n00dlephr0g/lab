services:
  filebrowser:
    container_name: zfilebrowser
    image: hurlenko/filebrowser
    user: 1000:1000
    ports:
      - ${PORT}:8080
    volumes:
      - /persist/zfilebrowser:/config
      # shares
      - /data/z:/data/z
    environment:
      - FB_BASE_URL=/
    restart: always
